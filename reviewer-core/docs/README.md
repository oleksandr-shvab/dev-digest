# reviewer-core/docs

Narrative documentation for `reviewer-core/`: architecture decisions, why
something is built the way it is, longer explanations that don't earn a
place in `CLAUDE.md`. Linked from `CLAUDE.md`, not auto-loaded.

Add one file per topic, e.g. `prompt-assembly.md`, `grounding.md`.
