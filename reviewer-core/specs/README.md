# reviewer-core/specs

Specs for `reviewer-core/`: expected behavior/contract of the pure engine
(e.g. single-pass vs map-reduce selection, grounding rules) — written before
or alongside implementation. Linked from `CLAUDE.md`, not auto-loaded.
