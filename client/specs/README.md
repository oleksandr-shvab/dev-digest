# client/specs

Feature/UI specs for `client/`: what a feature must do, its contract,
acceptance criteria — written before or alongside implementation. Linked
from `CLAUDE.md`, not auto-loaded.

Add one file per feature, e.g. `findings-triage.md`.
