# client/docs

Narrative documentation for `client/`: architecture decisions, why something
is built the way it is, longer explanations that don't earn a place in
`CLAUDE.md`. Linked from `CLAUDE.md`, not auto-loaded — read on demand.

Add one file per topic, e.g. `state-management.md`, `sse-and-polling.md`.
